<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.seedm.repository.mapper.seed.ISchoolRoleMapper">

    <insert id="insert" parameterType="SchoolRoleVo">
        INSERT INTO school_role
          (id, school_id, role_id)
        VALUES
          (#{id}, #{schoolId}, #{roleId})
    </insert>

    <!--<select id="selectById" resultType="RoleVo">-->
        <!--SELECT-->
          <!--id, name, sign, description-->
        <!--FROM-->
          <!--role-->
        <!--WHERE-->
          <!--id = #{id}-->
    <!--</select>-->
</mapper>